# Axiom Platform dependencies. Versions pinned to what has been verified
# working (2026-07-13). Install with: pip install -r requirements.txt

# Core runtime
anthropic==0.116.0
openpyxl==3.1.5
python-docx==1.2.0
lxml==6.1.1
pdfplumber==0.11.10
pandas==3.0.2
streamlit==1.57.0

# OCR lane (pdf_financial_extractor.py). Optional at runtime -- extraction
# degrades to a clear warning instead of failing if these, or a local
# Tesseract OCR install, are missing. See docs/OCR_LANE_DESIGN.md.
PyMuPDF==1.28.0
pytesseract==0.3.13
Pillow==12.2.0

# Test-only: build synthetic PDF/image fixtures in tests/test_financial_harvest.py
reportlab==5.0.0
numpy==2.4.4

# Windows-only: real Excel recalculation via COM (scripts/recalc_workbook.py).
# Formula-driven workbook tabs only have real cached values after Excel
# itself recalculates and saves; openpyxl cannot compute formulas.
pywin32==312
